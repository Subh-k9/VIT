
# 1. Steps t run the code
Open a terminal and run:
```bash
mkdir VIT
cd VIT
here you clone the repo inside this VIT flder you just have created

conda create -n vit python=3.10.
conda activate vit

pip install torch==2.5.0
pip install torchvision==0.20.0
pip install numpy==1.26.4


navigate through the VIT folder and run python main.py
please change the path in the main.py to  the data path where you will put the cifar_10 data_set
